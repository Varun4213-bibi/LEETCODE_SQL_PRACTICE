select today.id
FROM Weather yesterday INNER JOIN Weather today
on datediff(today.recordDate,yesterday.recordDate)=1
where today.temperature > yesterday.temperature

--used self-join to join weather table to itself creating two copies today and yesterday
and used datediff function to ensure today is 1 day after yesterday
then used condition for temp where today temp is greater than yesterday--
